name: Claude Agent Delegation - Database_Operations
on: [push, workflow_dispatch]

jobs:
  claude-agent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DeterminateSystems/nix-installer-action@main
        continue-on-error: true

      - name: Claude Agent - Database_Operations
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        continue-on-error: true
        run: |
          cd ~ && nix-shell -p nodejs expect --run 'expect -c "
            set timeout 900
            spawn npx @anthropic-ai/claude-code --dangerously-skip-permissions \"CLAUDE_Database_Operations\"
            expect { -re {.*bypass permissions.*} { send \"2\r\" } }
            expect { -re {.*analysis.*|.*complete.*|.*Write.*} { exit 0 } timeout { exit 1 } }
          "'

      - name: Ubuntu Fallback Agent
        if: failure()
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y nodejs npm curl
          echo "Ubuntu fallback: Database_Operations failed, documenting for retry" > fallback-0x07.txt
          echo "Repository: ${{ github.repository }}" >> fallback-0x07.txt
          echo "Task: CLAUDE_Database_Operations" >> fallback-0x07.txt
          echo "Status: Needs retry with enhanced configuration" >> fallback-0x07.txt
          curl -s ifconfig.me >> fallback-0x07.txt

      - name: Upload Agent Results
        uses: actions/upload-artifact@v4
        with:
          name: claude-agent-Database_Operations-0x07
          path: "*.txt"
          retention-days: 7